<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>پنل مدیریت</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-50">
  <div class="max-w-5xl mx-auto p-6">
    <div class="flex justify-between items-center mb-6">
      <h1 class="text-2xl font-bold">داشبورد</h1>
      <form method="POST" action="/admin/logout"><button class="px-3 py-2 bg-gray-800 text-white rounded">خروج</button></form>
    </div>

    <nav class="flex gap-3 mb-6">
      <a class="px-3 py-2 bg-white shadow rounded" href="/admin">آمار</a>
      <a class="px-3 py-2 bg-white shadow rounded" href="/admin/submissions">ثبت نام‌ها</a>
      <a class="px-3 py-2 bg-white shadow rounded" href="/admin/visits">بازدیدها</a>
      <a class="px-3 py-2 bg-white shadow rounded" href="/admin/editor">ویرایش سایت</a>
    </nav>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
      <div class="bg-white rounded shadow p-4">
        <div class="text-gray-500 text-sm">کل بازدیدها</div>
        <div class="text-2xl font-bold"><%= totalVisits %></div>
      </div>
      <div class="bg-white rounded shadow p-4">
        <div class="text-gray-500 text-sm">بازدیدهای امروز</div>
        <div class="text-2xl font-bold"><%= todayVisits %></div>
      </div>
      <div class="bg-white rounded shadow p-4">
        <div class="text-gray-500 text-sm">کل ثبت‌نام‌ها</div>
        <div class="text-2xl font-bold"><%= totalSubmissions %></div>
      </div>
    </div>

    <div class="bg-white rounded shadow p-4">
      <h2 class="font-bold mb-3">۱۰ ثبت‌نام اخیر</h2>
      <div class="overflow-x-auto">
        <table class="min-w-full text-sm">
          <thead>
            <tr class="text-right">
              <th class="p-2">نام</th>
              <th class="p-2">تلفن</th>
              <th class="p-2">ایمیل</th>
              <th class="p-2">خدمات</th>
              <th class="p-2">شماره مشتری</th>
              <th class="p-2">تاریخ</th>
            </tr>
          </thead>
          <tbody>
          <% last10.forEach(s => { %>
            <tr class="border-t">
              <td class="p-2"><%= s.name %></td>
              <td class="p-2"><%= s.phone %></td>
              <td class="p-2"><%= s.email %></td>
              <td class="p-2"><%= s.services %></td>
              <td class="p-2"><%= s.customer_number %></td>
              <td class="p-2"><%= s.submission_date %></td>
            </tr>
          <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
</html>
