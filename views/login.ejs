<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>ورود مدیر</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-100">
  <div class="min-h-screen flex items-center justify-center p-4">
    <form method="POST" action="/admin/login" class="bg-white shadow rounded-xl p-6 w-full max-w-sm space-y-4">
      <h1 class="text-xl font-bold text-center">ورود مدیر</h1>
      <% if (error) { %>
        <div class="text-red-600 text-sm"><%= error %></div>
      <% } %>
      <div>
        <label class="block text-sm mb-1">نام کاربری</label>
        <input name="username" value="admin" class="w-full border rounded px-3 py-2" />
      </div>
      <div>
        <label class="block text-sm mb-1">رمز عبور</label>
        <input name="password" type="password" class="w-full border rounded px-3 py-2" />
      </div>
      <button class="w-full bg-indigo-600 text-white rounded py-2">ورود</button>
    </form>
  </div>
</body>
</html>
